import { ObjectID as ObjectIDType } from "./Database";

// Set the user onto a category.
function SelectCategory(CategoryID: ObjectIDType) {
    // pass
}

// After a user joins a category, sort them into a queue.
function SortUser(UserID: ObjectIDType) {
    // pass
}

// After a queue fills up, send the message or set the results to the user profile.
function FulfillQueue(QueueID: ObjectIDType) {
    // pass
}
